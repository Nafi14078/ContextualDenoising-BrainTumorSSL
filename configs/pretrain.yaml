seed: 42

dataset_path: /kaggle/input/datasets/ashfakazadnafi/brats2021-tar/datasets/brats2021

save_dir: outputs/checkpoints/
log_dir: outputs/logs/

device: cuda

image_size: 128

modalities:
  - t1
  - t1ce
  - t2
  - flair

num_input_slices: 5

batch_size: 8
epochs: 60

learning_rate: 0.0001
weight_decay: 0.00001

num_workers: 4

noise_std: 0.1
mask_ratio: 0.15

use_amp: true

train_split: 0.9